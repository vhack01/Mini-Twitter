const NoBookmarks = () => {
  return (
    <div className="flex flex-col items-center">
      <h1 className="text-lg font-bold">Save posts for later</h1>
      <p className="text-sm">
        Bookmark posts to easily find them again in the future.
      </p>
    </div>
  );
};

export default NoBookmarks;
